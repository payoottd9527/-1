第四步：对账与资金管理（后台闭环）
用户用卡消费后，你需要处理：

模块	功能	实现方式
交易监控	实时获取用户消费记录	订阅webhook事件（如issuing.report.succeeded）
余额管理	用户预充值、实时扣款	用户先充值到你的平台，你再给卡充值
对账报表	生成对账单	汇总每张卡的交易记录，支持导出
自动分账	平台手续费扣除	每笔交易抽取一定比例作为平台收入
{
  "event_type": "issuing.report.succeeded",
  "data": {
    "report_id": "ba36ef65-c0f3-4aa4-9854-a0d644936418",
    "secret_key": "加密密钥"
  }
}
用户注册
   ↓
实名认证（KYC）← 调用第三方KYC服务
   ↓
审核通过（个人/企业）
   ↓
用户申请卡片 ← 选择卡片类型（银联/其他）
   ↓
调用发卡商API创建卡片
   ├─ POST /clients（创建客户）
   ├─ POST /accounts（建立账户）
   └─ POST /cards（发卡）
   ↓
卡片信息加密存储 ← 你的数据库
   ↓
用户登录查看卡片 ← 显示卡号后4位
   ↓
用户充值 ← 你的支付网关
   ↓
卡片激活可用
   ↓
用户消费 ← 交易通过webhook通知你
   ↓
实时扣减余额
   ↓
生成对账单
